- block:
    - name: Recursos iniciales de la VM {{ vmostype }} - {{ vmname }} en VirtuaBox
      debug:
        msg:
          - "Tipo: {{ vmostype }}"
          - "Nombre: {{ vmname }}"
          - "Vitualizador: VirtuaBox"
          - "Ambiente {{ vmenv.split(',')[0] | trim }}"
          - "Numero de CPU's: {{ vmsource.split(',')[0] | trim }} "
          - "Cantidad de Memoria: {{ vmsource.split(',')[1] | int * 1024 }} GB"
          - "Tamaño disco extra: {{ vmsource.split(',')[2] | int * 1024 }} GB"