- block:
    - name: Recursos finales de la VM {{ vmostype }} - {{ vmname }} en VirtuaBox
      debug:
        msg:
          - "La VM {{ vmostype }} - {{ vmname }} en VirtuaBox - Activa y con los recursos:" 
          - "Tipo: {{ vmostype }}"
          - "Nombre: {{ vmname }}"
          - "Vitualizador: VirtuaBox"
          - "Ambiente {{ vmenv.split(',')[0] | trim }}"
          - "Numero de CPU's: {{ vmsource.split(',')[0] | trim }} "
          - "Cantidad de Memoria: {{ vmsource.split(',')[1] | trim }} MB"
          - "Tamaño disco extra: {{ vmsource.split(',')[2] | trim }} MB"